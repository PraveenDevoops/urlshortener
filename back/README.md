# urlshortener-back
Backend service for URL Shortener

Environment variables:

DB_USER  
DB_PASSWORD  
DB_SERVER i.e. OpenShift Service name  

Runs on port 3000  

Database is "urls".
Table is "routes".
